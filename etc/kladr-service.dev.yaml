server:
    applicationConnectors:
        - type: http
          port: 3000
          bindHost: 127.0.0.1
    adminConnectors:
        - type: http
          port: 3001
          bindHost: 127.0.0.1

# Logging settings.
logging:

    # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
    level: INFO

    # Logger-specific levels.
    loggers:
        org.jamel.kladr: DEBUG

    appenders:
        - type: console

archivePath: /Users/mac/Downloads/BASE.7Z
extractPath: /tmp/kladr
indexPath: ./indexes/kladr.idx
